# Application
APP_ENV=development
PORT=8080
FRONTEND_URI=http://localhost:3000
LOG_LEVEL=info

# MongoDB
MONGODB_URI=mongodb://localhost:27017/?replicaSet=rs0&directConnection=true
MONGODB_NAME=rcthub

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT
JWT_SECRET=change-me-to-a-random-secret-at-least-32-bytes
JWT_EXPIRY_HOURS=168

# osu! OAuth
OSU_CLIENT_ID=
OSU_CLIENT_SECRET=
OSU_REDIRECT_URI=http://localhost:8090/auth/osu/callback
OSU_API_BASE=https://osu.ppy.sh

# osu! Fetcher (optional — defaults: 30min user cache, 24h beatmap cache)
OSU_FETCHER_USER_CACHE_TTL_MIN=30
OSU_FETCHER_BEATMAP_CACHE_TTL_HR=24

# CORS
ALLOWED_ORIGINS=http://localhost:3000
